{
  "name": "plum-root",
  "version": "1.0.0",
  "description": "Root package to manage Plum implementation",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "server": "cd server && npm start",
    "app": "npm run build && npm run server",
    "push": "git add . && git commit -m \"Update\" && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
